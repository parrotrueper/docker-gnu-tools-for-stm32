# Build the ARM toolchain with STM32 patches

## Requirements

Docker Engine

## To build the toolchain

`./build-arm-stm32.sh`

The result will be a pkg directory with a tar ball and the md5 text file.
